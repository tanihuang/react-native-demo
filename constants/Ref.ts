import { createNavigationContainerRef } from "@react-navigation/native";
import { createRef } from 'react';

export const navigationRef = createNavigationContainerRef<any>();
export const drawerContentRef = createRef<any>();